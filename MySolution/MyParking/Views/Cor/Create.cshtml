@model MyParking.DAL.Models.CorVeiculo

@{
    ViewBag.Title = "Create";
}

<h2>Criar Nova Cor</h2>

@using (@Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Cor do Veículo</legend>

        <div class="display-label">
            @Html.DisplayNameFor(model => model.corVeiculo)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.corVeiculo)
            @Html.ValidationMessageFor(model => model.corVeiculo)
        </div>
        <p>
            <input type="submit" value="Salvar" />
        </p>
    </fieldset>
}

<div >@Html.ActionLink("Voltar", "Index")</div>